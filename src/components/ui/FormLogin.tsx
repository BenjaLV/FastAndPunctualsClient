import React, { ReactNode } from 'react'


interface Props {
    children: ReactNode;
}


const FormLogin = ({children}: Props) => {
  return (
    <div>FormLogin</div>
  )
}

export default FormLogin